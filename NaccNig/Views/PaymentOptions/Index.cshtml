@using NaccNigModels.Payments
@model IEnumerable<NaccNigModels.Payments.PaymentOptions>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PaymentOptionsName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PaymentOptionsName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PaymentOptionsId }) |
            @Html.ActionLink("Details", "Details", new { id=item.PaymentOptionsId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PaymentOptionsId })
        </td>
    </tr>
}

</table>
