@model IEnumerable<NaccNig.Models.Payments.MonthlyDues>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ActiveMember.StateOfDeployment)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IsPaidMonthlyDues)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PaymentStatus)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ActiveMember.StateOfDeployment)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IsPaidMonthlyDues)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PaymentStatus)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MonthlyDuesId }) |
            @Html.ActionLink("Details", "Details", new { id=item.MonthlyDuesId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MonthlyDuesId })
        </td>
    </tr>
}

</table>
